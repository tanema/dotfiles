These are the setups for the LSPs I use. The language servers that are required
are installed using the global brewfile. If I add a new language server here, I
need to add it to the brewfile as well.

Setup new LSPs honestly by just going to https://github.com/neovim/nvim-lspconfig,
finding their config and copying most of it.
